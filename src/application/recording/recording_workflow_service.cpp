#include "application/recording/recording_workflow_service.h"

// Provides the initial native recording workflow scaffold for later media integration.

namespace travis::application::recording {

RecordingWorkflowService::RecordingWorkflowService(QObject* parent)
    : QObject(parent) {}

} // namespace travis::application::recording
